---
import Layout from '../components/Layout.astro';
import PostCard from '../components/PostCard.astro';
import { SITE_TITLE, HERO, BASE } from '../consts';
import { getCollection } from 'astro:content';
import { stripMarkdown } from '../utils';

const essays = (await getCollection('essays'))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3);

const notes = (await getCollection('notes'))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3);
---

<Layout title="主页">
  <!-- Hero Section -->
  <section class="py-10 sm:py-24 flex items-center justify-between gap-4 sm:gap-8">
    <div class="flex-1 text-left">
      <h1 class="text-3xl sm:text-5xl font-bold mb-3 sm:mb-4 text-[#1A1A1A] dark:text-white">
        {HERO.greeting}
      </h1>
      <p class="text-base sm:text-lg text-[#666666] max-w-md dark:text-gray-400">
        {HERO.description}
      </p>
      <div class="mt-6 sm:mt-8 flex items-center gap-3 sm:gap-4">
        <a href={`${BASE}/daily`} class="inline-block px-6 py-2.5 bg-primary text-white rounded-lg font-medium hover:bg-primary-dark transition-colors dark:text-white">去看随笔</a>
        <a href={`${BASE}/fun`} class="inline-block px-6 py-2.5 border-2 border-primary text-primary rounded-lg font-medium hover:bg-primary hover:text-white transition-colors dark:border-red-400 dark:text-red-400 dark:hover:bg-red-500 dark:hover:text-white dark:hover:border-red-500">
          玩个小游戏
        </a>
      </div>
    </div>
    <div class="hidden sm:block flex-shrink-0">
      <img src={`${BASE}/images/t1.jpg`} alt="avatar" class="w-28 h-28 rounded-full object-cover shadow-lg" />
    </div>
  </section>

  <!-- Recent Essays -->
  <section class="mb-12">
    <div class="flex items-center justify-between mb-6">
      <h2 class="text-2xl font-bold text-[#1A1A1A] dark:text-white">
        近期随笔
      </h2>
      <a href={`${BASE}/daily`} class="text-sm text-primary hover:text-primary-dark dark:text-red-400 dark:hover:text-red-300">查看全部 →</a>
    </div>
    {
      essays.length > 0 ? (
        <div class="grid gap-4">
          {essays.map((essay) => (
            <PostCard
              title={essay.data.title}
              date={essay.data.date}
              href={`${BASE}/daily/${essay.id}`}
              type="essay"
              excerpt={stripMarkdown(essay.body ?? '')}
            />
          ))}
        </div>
      ) : (
        <p class="text-[#666666] text-sm dark:text-gray-400">暂无随笔，去后台写一篇吧 ✍️</p>
      )
    }
  </section>

  <!-- Recent Notes -->
  <section class="mb-12">
    <div class="flex items-center justify-between mb-6">
      <h2 class="text-2xl font-bold text-[#1A1A1A] dark:text-white">
        最近笔记
      </h2>
      <a href={`${BASE}/notes`} class="text-sm text-primary hover:text-primary-dark dark:text-red-400 dark:hover:text-red-300">查看全部 →</a>
    </div>
    {
      notes.length > 0 ? (
        <div class="grid gap-4">
          {notes.map((note) => (
            <PostCard
              title={note.data.title}
              date={note.data.date}
              href={`${BASE}/notes/${note.id}`}
              type="note"
              excerpt={stripMarkdown(note.body ?? '')}
            />
          ))}
        </div>
      ) : (
        <p class="text-[#666666] text-sm dark:text-gray-400">暂无笔记，去后台写一篇吧 ✍️</p>
      )
    }
  </section>
</Layout>
